VITE_API_BASE_URL=http://localhost:5000/api

# Optional: Google AI Studio key for live Gemini on the AI Mentor chat page.
# Prefer a backend proxy in production — never expose a production key in the client.
VITE_GEMINI_API_KEY=
VITE_GEMINI_MODEL=gemini-2.0-flash

